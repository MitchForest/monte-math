# Monte Math database tooling configuration
# Used by migration scripts and Kysely code generation.

# Primary Postgres connection string (Supabase or local dev)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Optional overrides
DATABASE_SSL_MODE=disable
DATABASE_SCHEMA=public
