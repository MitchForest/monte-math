{
  "name": "@monte-math/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:",
    "@monte-math/db": "workspace:*",
    "@monte-math/knowledge-graph": "workspace:*",
    "@monte-math/learning-engine": "workspace:*",
    "@monte-math/shared": "workspace:*",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "bun-types": "^1.2.23",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
